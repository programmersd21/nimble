# Nimble Standard Library — Threading support

load std.async::{spawn, join, sleep}

struct Thread:
    let id: Int = 0

fn new[T](f: fn() -> T) -> Thread:
    return Thread{id: spawn(f)}

fn join(self: Thread) -> Void:
    join(self.id)
