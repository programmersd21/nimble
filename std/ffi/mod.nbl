# Nimble Standard Library - Foreign Function Interface
#
# Provides C library bindings for common system calls.

extern fn nimble_exit(code: Int) -> Void

fn system_exit(code: Int) -> Void:
    nimble_exit(code)
