# Nimble Standard Library - Panic and assertion primitives

extern fn nimble_panic(msg: String) -> Void

fn panic(msg: String) -> Void:
    nimble_panic(msg)

fn assert(condition: Bool, msg: String) -> Void:
    if !condition:
        nimble_panic(msg)
