# Nimble Booleans Demo
# Boolean logic with and, or, not

extern fn nimble_print_i64(x: Int) -> Void

fn test_and(a: Bool, b: Bool) -> Bool:
    return a && b

fn test_or(a: Bool, b: Bool) -> Bool:
    return a || b

fn test_not(flag: Bool) -> Bool:
    return !flag

fn main() -> Int:
    let t = true
    let f = false

    let r1 = test_and(t, t)
    let r2 = test_or(f, f)
    let r3 = test_not(f)

    if t && !f:
        nimble_print_i64(1)

    if f || t:
        nimble_print_i64(2)

    return 0
